var observableModule = require("data/observable");

function PageSelectionViewModel() {
	var viewModel = observableModule.fromObject({
	});

	return viewModel;
}

module.exports = PageSelectionViewModel;
